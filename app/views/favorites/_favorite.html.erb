  <% if user_signed_in? && recipe.favorited_by?(current_user)%>
    <%= link_to '<i class="bi bi-heart-fill" style="font-size: 3em; color: #db7093;"></i>'.html_safe, recipe_favorites_path(recipe.id),  data: { turbo_method: :delete }, class: "recipe_favorites", remote: true %>
  <% else %>
    <%= link_to '<i class="bi bi-heart" style="font-size: 3em; color: #db7093;"></i>'.html_safe, recipe_favorites_path(recipe.id), data: { turbo_method: :post }, class: "recipe_favorites", remote: true %>
  <% end %>